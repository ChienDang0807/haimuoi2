spring:
  config:
    activate:
      on-profile: dev
  devtools:
    add-properties: true
  data:
    mongodb:
      uri: mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27010}/${MONGO_DB:haimuoi2-mongo}?authSource=admin

openapi:
  service:
    api-docs: cart-service
    server: http://localhost:${server.port}
    title: cart service
    version: 1.0.0